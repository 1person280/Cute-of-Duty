# 冻结任务 · 0.6-Snapshot-8 实机反馈

> **本目录的用途**：`main` 分支的 [stop-doing.md](../stop-doing.md) 冻结区在当前工作分支
> （`wip/0.8-snapshot-8`）不存在，且本轮不合并 `main`。为避免"实测结论丢在聊天里"，
> 本目录专门承接**当前工作分支上的冻结条目**——语义与 stop-doing.md 一致：
>
> 1. 本目录列出的条目，在移出本目录前**不得**在 README / CHANGELOG / release note 里写成"已完成"；
> 2. **不得**在其基础上继续叠加新玩法代码（先验证，再往前走）；
> 3. 解除冻结必须补上"验证方式 + 实测结果"，并把条目迁回 stop-doing.md / barek-history。

最后更新：2026-09-26（0.6-Snapshot-9 修复落地 · owner）

> **本轮（Snapshot-9）处置**：C 节四项**代码已全部落地并通过 `cargo-wrap check --workspace`
> （退出码 0）与 `cargo-wrap test -p cute_of_duty_server`（**106 passed / 0 failed**）**。
> 但**实机复测尚未进行**——按本目录规矩，条目在"验证方式 + 实测结果"补齐前**不移出本目录**，
> 状态记为「⏳ 待实机复测」。B 节按用户指示**整体推迟**到下一快照。
>
> **规格澄清**：A#4 / C.3 原文写「鼠标拖拽 / **Shift+右键**」，与既有仓库选装
> （`menu/arsenal.rs`）的 **Shift+左键**冲突。经用户裁决，**统一采用 `Shift+左键`**
> （修正原文的「右键」表述）；三处（物资箱 / 仓库 / 补给台）形态一致，仓库无需改动。

---

## A. 快照8 实机反馈 · 状态

| # | 反馈原文 | 代码状态 | 实测状态 | 遗留 / 卡在哪 |
|---|---|---|---|---|
| 1 | 「3/4 改成消耗品 **没有名字**，且**没有轮盘切换**」 | `HostCode/hud/hud_item_wheel.rs` 已实现长按径向轮盘 + 短按速用；`hud_vitals.rs` 3/4 槽应显示"物品名 ×N" | ⏳ 待实机复测 | **已修**：`held_key` 残留路径堵死（见 A-1）；无可用物品时推 HUD 播报「XX：没有可用物品」（不再静默） |
| 2 | 「两个武器槽只能用一个且与干员耦合」 | 已与干员解耦 | ✅ 用户确认 | — |
| 3 | 「F 交互没有鼠标滚轮切换的翻页面板，**必须按 F 才能看到**；且补给台**不能按照物品名字给予**」 | `hud_interact.rs` 改就近常显 + 滚轮翻页；补给台按 `SupplyKind::label()` 物品名给予 | ✅ **已解决**（用户确认） | ~~遗留：物品种类太少~~ → **已处理**：物资箱掉落池 12 → 20（见 C.4） |
| 4 | 「起始点**没有可交互箱子**；不是开启直接拿走，而是**两个 4×3 界面**（仓库也改成两个 4×3）」 | 新增 `hud_loot_panel.rs`（容器↔背包 4×3 逐格转移）；`menu/arsenal.rs` 仓库/背包均改 4×3 | ✅ **已还原**（用户确认"问题不大"） | **本轮已统一**：物资箱 / 补给台改为与仓库同形态拖拽面板；Shift 快捷移动统一为 **Shift+左键**（见 C.3） |
| 5 | 「去除窗口右边的若干干员」 | 已移除 | ✅ 用户确认 | — |
| 6 | 「**WASD 无法移动**」 | 已修（双路径，见 A-1） | ⏳ 待实机复测 | **已修**：①轮盘 `held_key` 残留；②二级面板被轮盘残留卡住无法 Esc 收起 |

### A-1. 「WASD 无法移动」根因与修复（Snapshot-9 已落地，待实机复测）

实机现象（快照8）：进训练场后 WASD / 开火 / 鼠标视角全灭。

**根因链（两个互相放大的缺陷）**：

1. **轮盘 `held_key` 残留**（`HostCode/hud/hud_item_wheel.rs`）：原实现把"松开 3/4"的处理放在
   `if !state.open && blocked { return; }` **之后**。若在按住 3/4 期间被模态（F/M/暂停）打断，
   松开事件被吞 → `held_key` 永不复位 → `held` 持续累积 → 在**无按键**时 `state.open` 自发置真并**永久卡死**。
2. **二级面板无法 Esc 收起**（`HostCode/hud/hud_interact.rs`）：`interact_input` 里
   `if loot.open || wheel_state.open { return; }` 排在 Esc 处理**之前**，一旦轮盘（或面板）异常残留
   `open`，Esc 被让位吞掉 → `interact.panel_open` 无法清除 → `gameplay_input_active` 恒假。

**修复（Snapshot-9）**：

- `item_wheel_input` 重写为三段式：**① 松开优先结算**（不受任何门控影响，是本次会话的唯一出口）；
  **② 键已不再按住却仍在会话中**（窗口失焦等丢事件）→ **兜底复位**；**③ 其它模态接管 → 整体丢弃会话**。
- `interact_input` 把 **Esc 关闭二级面板提到轮盘/物资箱让位之前**，任何残留 `open` 都能被收起。

> 附带说明：报告称"二级面板在无输入下自行展开"。全仓穷举确认 `open_panel` **唯一**置位路径是
> `confirm_entry ← interact_input` 的 `F/Enter just_pressed`，故"自开"实为测试者按 F，
> 真正缺陷是随后**被轮盘残留卡死**（Esc 失效）。该路径已随上述修复关闭。

---

## B. 快照8 未完成项（发布时明确"还差"的）

> **本轮整体推迟**（用户指示"下一快照目标拖到下一快照"）：以下两项**不在 Snapshot-9 范围内**，
> 原样顺延到下一快照，本轮不做任何实现。

| # | 项 | 状态 |
|---|---|---|
| 1 | **按钮**（demo 操作方法的 UI 触发） | ⏳ 推迟（本轮定为"仅核对，不实现"） |
| 2 | 扁平化剩余目标：**B. legacy demo 操作表逐行核对**（基准 `_ref/Cute-of-Duty-0.3.2/README.md`） | ⏳ 推迟（逐行核对结论另记） |

---

## C. 本轮要修（0.6-Snapshot-9 实施范围与结果）

| # | 项 | 代码 | 验证 |
|---|---|---|---|
| 1 | **修 WASD 冻结**：清「交互二级面板无故自开 / 无法 Esc 收起」与「`item_wheel` `held_key` 残留」两条路径 | ✅ 已落地（见 A-1） | `cargo-wrap check --workspace` 退出码 0；⏳ 实机复测 |
| 2 | **修 3/4 消耗品无响应**：短按速用首件 / 长按径向轮盘，均有可见反馈 | ✅ 已落地（短按/长按/无物品播报） | ⏳ 实机复测 |
| 3 | **统一物品搬运交互**：物资箱 / 仓库 / 补给台 全部为鼠标拖拽 + Shift 快捷移动（**经裁决取 `Shift+左键`**） | ✅ 已落地：物资箱/补给台改用 `hud_loot_panel` 双向 4×3 拖拽（新增 `loot_panel_drag`）；补给台 `LootMode::Supply`；仓库（arsenal）原有拖拽形态保持，三处一致 | ⏳ 实机复测 |
| 4 | **扩物品表**（A 表 #3 遗留） | ✅ 已落地：`ServerCode/items/mod.rs` 掉落池 **12 → 20**（六元素手雷/步枪各一 + 弹药 ×30/×60/×90 + 医疗包/大型/急救 + 护甲片/重型护甲板），无协议变更 | `cargo-wrap test -p cute_of_duty_server` 106 passed / 0 failed |

### C-1. 实机复测清单（待用户执行，通过后才可迁出本目录）

服务端 `cod_server.exe` + 客户端 `cod1.exe`（**先起服务端**），逐项确认：

1. **WASD 冻结**：进训练场后能立即用 WASD 移动、鼠标转视角、左键开火；按 F 打开二级/格位面板后
   再按 **Esc** 能收起，收起后玩法输入立即恢复。
2. **3/4 消耗品**：短按 `3` 应立刻用掉一个医疗包并回血、HUD 3 槽计数 -1，左下播报「使用 医疗包」；
   短按 `4` 投出手雷；**长按** `3`/`4` 0.25s 呼出径向轮盘，鼠标划方向高亮、松开使用、靠中心松手取消；
   该类无物品时播报「恢复类(3)：没有可用物品」。
3. **搬运统一**：物资箱 / 补给台靠近按 F → 出现**两个 4×3 网格**；**左键拖拽**左格到右网格松手 = 取入，
   拖回 = 放回；**Shift+左键** = 快捷移动；补给台左网格为 `步枪弹药 ×90 / 医疗包 / 护甲片`，拖入背包即领取。
4. **物品表**：不同物资箱内容更有变化（应能看到火焰/冰霜/雷电/毒素/破片/水压六系手雷与步枪、
   多档弹药与医疗/护甲）。

---

## D. Snapshot-9 试玩回执（本轮新增 · 待复测）

用户实测（0.6-Snapshot-9 release）：**"要不是左上角有提示，我还以为没改"** —— 提示表明 3/4 的
按键链路已接通，但**HUD 3/4 计数未变、手雷无投掷**；另反馈**物资箱面板"依旧别扭"且鼠标未被释放、
无法拖拽**。用户处置指示：**计数不更新 → 修；无投掷 → 登记 README 已知问题**。

| # | 反馈 | 处置 | 状态 |
|---|---|---|---|
| 1 | **格位面板不释放鼠标**（"没有呼出鼠标让我拖拽"/箱子别扭） | ✅ **已修**（本轮）`menu/pause.rs::cursor_lock_system` 原**只认暂停**，物资箱/补给台 4×3 面板、交互二级选项面板、战术大地图打开时鼠标仍 `CursorGrabMode::Locked` 且隐藏 → bevy `ui_focus_system` 只在窗口中心命中节点，无法悬停/拖拽；现**任一指针型面板打开即释放光标并显示指针**（径向轮盘除外）。**注：此状态会让 `gameplay_input_active` 恒假 → 玩家若被卡在面板里，3/4 上报被冻结，表现正是"计数不变+无投掷"，故本条同时是 #2 的候选根因。** | ⏳ 待实机复测 |
| 2 | **3/4 使用后 HUD 计数不更新**（用户二次澄清：**"仓库是有的，使用后没有正常消耗，所以不更新"**） | ✅ **根因已定位并修复**（本轮，推翻先前"模态冻结"猜测）：`hud_item_wheel.rs::item_wheel_input` 的**三条结算路径**（松开结算 / 同帧点按 / 兜底复位）都是**先写 `pending_slot`、紧接着 `*state = ItemWheelState::default()`**；`default()` 的 `pending_slot` 为 `None`，**把刚写好的格位原地抹掉** → `net::input_system` 恒取到 `None` → `PlayerInput.use_slot` 恒为 `None` → 服务端收不到使用意图 → **数量不减、也不投掷**（左上角提示由 `announce_use` 先于覆盖打出，故"有提示但没生效"）。修法：新增 `ItemWheelState::reset_session()`（只清会话字段、**保留 `pending_slot`**），会话收尾统一走它。 | ⏳ 待实机复测 |
| 2b | **格位面板无拖拽预览**（"没有预览，**仓库是有的**"） | ✅ **已修**（本轮）`hud_loot_panel.rs` 新增 `LootGhost`/`LootGhostText`：`loot_panel_drag` 在拖拽期间**跟随光标显示被拖物资名**，与仓库选装 `arsenal.rs` 同形态（此前格位面板无幽灵，拖拽**无任何视觉反馈** → "拖着没反应"）。 | ⏳ 待实机复测 |
| 3 | **手雷无可见投射物** | ✅ **已登记 README 已知问题**（服务端已生成投射物，疑为未进快照 / 客户端未渲染），按其指示**非本轮修复范围**。 | 🔒 已知问题 |
| 4 | **新 feature（用户指定）**：备用子弹改为**背包物品**、可**堆叠 64**（部分物品上限 16，工具不可堆叠） | 📌 **已登记为下一快照 feature**（与 B 节「按钮」更新同期落地），并写入 README 版本行。 | ⏳ 下一快照 |

### D-1. 复测重点（本轮新增）

1. **鼠标释放**：靠近物资箱/补给台按 F → 光标应**立即可见可移动**；悬停格子应高亮、左键可拖拽搬运、
   Shift+左键快捷移动；按 Esc / 移出面板后光标应重新锁定（相机恢复跟随）。
2. **拖拽预览**：左键按住格子拖动时，光标处应出现**幽灵物资名**跟随（与仓库选装一致）。
3. **3/4 计数**：确认**无面板打开**（光标处于锁定隐藏、相机可跟随鼠标）时短按 `3`：HP 应回血、
   HUD 3 槽计数应 -1；短按 `4` 计数 -1。（若计数仍不变，请回报"按下 3 后 HP 是否变化"+ 左上角提示原文）
