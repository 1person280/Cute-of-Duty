# stop-doing.md · 冻结区

> **这个文件的唯一用途**：把所有"**改了但没验证**""**想改但先别改**""**已确认不动**"的事项
> 集中冻结，防止它们被当成已完成、或在其上继续叠加新代码（屎山的主要成因就是"在未验证的地基上盖楼"）。
>
> **规则**：本文件列出的条目，在移出本文件之前——
> 1. 不得在 README / CHANGELOG / 任何 release note 里被描述为"已完成"；
> 2. 不得在其基础上继续重构或新增功能（先验证，再往前走）；
> 3. 解除冻结必须补上"验证方式 + 实测结果"，并把条目移到 [barek-history.md](barek-history.md)。

最后更新：2026-09-26（架构转向裁决 · 代码全面暂停；追加解冻前置：**先清理多余文件，再做冻结需求**）

---

## 0. 本轮冻结裁决（owner，2026-09-26）

| # | 裁决项 | 结论 |
|---|---|---|
| 1 | `items`(格位制) 与 `inventory`(索引制+装备注册表) 两套背包 | **方案 A「按域切开」**：局内归 `items`/`combat`（不落盘），局外归 `inventory`/`equipment`（落盘）。见 [ADR 0002](adr/0002-backpack-domain-split.md) |
| 2 | 客户端跨 crate 直连服务端具体类型 | **抽独立契约 crate `cute_of_duty_contract`**；`HostCode` 不再依赖 `ServerCode`（判据 = `HostCode/Cargo.toml` 删掉该依赖）。见 [ADR 0003](adr/0003-contract-crate.md) |
| 3 | `hud` 横向依赖 `menu` | **`flow` 拥有唯一 `ModalState` + 事件仲裁**；`hud`/`net` 只读 `blocks_gameplay_input()`；出现 `use crate::menu::` 即违规。见 [ADR 0004](adr/0004-client-layer-convergence.md) |
| 4 | `launcher` 装配层兼任表现宿主 | **本轮一起瘦身**：只留 ①装 dylib ②init ③`add_systems` ④`run`；表现代码归还 `world`/`hud`/`menu`。见 [ADR 0004](adr/0004-client-layer-convergence.md) |
| 5 | 接下来先做什么 | **全面暂停代码** —— 先补 `module.md` + 契约 YAML + 事件清单，再统一开工 |
| 6 | 建在未编译代码上的 A1/A3/A4 三批改动 | **保持冻结不动**（不修、不删、不继续叠加） |

> 暂停期间**唯一允许推进**的工作：补 `module.md`、补契约 YAML、补事件清单、写 ADR（纯文档，零代码）。
> 清单见 [module-boundaries 第七节](architecture/module-boundaries.md)。

---

## A. 用户反馈 5 条 · 状态

| # | 反馈原文 | 代码状态 | 验证状态 | 卡在哪 |
|---|---|---|---|---|
| 1 | 「3/4 改成消耗品 **没有名字**，且**没有轮盘切换**」 | 已改：新增 `HostCode/hud/hud_item_wheel.rs`（长按径向轮盘 + 短按速用），`hud_vitals.rs` 3/4 槽改为显示"物品名 ×N" | ❌ **未验证** | 未编译（客户端本轮改动零编译）；轮盘与 `side/pending_slot` 链路未实测 |
| 2 | 「两个武器槽只能用一个且与干员耦合」 | 已确认修复 | ✅ 用户确认 | — |
| 3 | 「F 交互没有鼠标滚轮切换的翻页面板，**必须按 F 才能看到**；且补给台**不能按照物品名字给予**」 | 已改：`hud_interact.rs` 删 `list_open` 改就近常显；`hud_interact_panel.rs` 改可见性条件；`ServiceCode/interact/mod.rs` 的 `SupplyKind::label()` 改按物品名 | ❌ **未验证** | 未编译；"就近常显小面板"的新布局（`left 55% / top 40%`）是否遮挡准星未实测 |
| 4 | 「起始点**没有可交互箱子**；不是开启直接拿走，而是**两个 4×3 界面**（仓库也改成两个 4×3）」 | 已改：新增 `hud_loot_panel.rs`（容器↔背包 4×3 逐格转移）；`menu/arsenal.rs` 仓库与背包均改 4×3 | ❌ **未验证** | 未编译；服务端 12 格容器/背包转移协议未联调 |
| 5 | 「去除窗口右边的若干干员」 | 已确认修复 | ✅ 用户确认 | — |

> ⚠️ 1/3/4 是**同一批未编译的改动**。当前 `cargo-wrap check --workspace` **尚未跑过**，
> 客户端此前还引用着服务端已删除的 `EntitySnapshot.medkit / .grenade`，因此**现在必然编译失败**——
> 这是预期断层，不是新问题，但必须先修通才能谈验证。

---

## B. legacy demo 操作表逐行核对（单机时代 · `_ref/Cute-of-Duty-0.3.2/README.md`）

对照基准是 legacy 操作表的 13 行。**凡是"未验证"的行，都不许在文档里写成已还原。**

| 按键组 | legacy 行为 | 现状 | 判定 |
|---|---|---|---|
| 视角 | 鼠标自由视角，X/Y 灵敏度**独立**，俯仰限 **仰 50° / 俯 70°** | `world/camera.rs` 有自由视角 | ⚠️ 灵敏度是否独立、俯仰限值是否一致 **未验证** |
| 移动 | WASD，**始终相对相机方向** | 已实现，快照驱动 | ⚠️ "相对相机"轴系在越肩状态下 **未验证** |
| 跳跃 | **Space**（仅在地面时） | 未见服务端跳跃链路 | ❌ **未实现 / 未验证** |
| 疾跑 | **左 Ctrl**，常速 **4 → 7 单位/秒**（按住） | 客户端绑的是 **`ShiftLeft`** | ❌ **键位偏差**；速度 4→7 是否一致 **未验证** |
| 主武器切换 | 1 / 2 | 已实现（与干员解耦） | ⚠️ 未联调实测 |
| 干员技能 | Q / E | 已实现 | ⚠️ 未联调实测 |
| 越肩瞄准 | 右键按住；SpringArm **6.5m → 2.4m，0.22s**；FOV **−28%**；准星**琥珀**；移速 **55%**；**碰撞避障**（撞墙缩回贴墙 ≥0.7m、离墙缓伸、地面钳制 ≥0.35m） | 相机过渡/FOV/准星/移速已有代码 | ❌ **SpringArm 碰撞避障 未实现/未验证**；0.22s 与 55% 数值 **未验证** |
| 射击 / 投掷 | 左键射击；**持雷时左键改为投掷** | 射击已实现 | ❌ **持雷切换投掷 未实现** |
| 交互 | **F** 呼出统一菜单（功能台 + 拾取物，**站点优先**）；**滚轮选择，F 确认** | 本轮改为**就近常显**（不再是"按 F 才看到"） | ⚠️ **与 legacy 语义有意偏离**（用户要求），需在验证后确认手感 |
| 背包 | **Tab** 打开背包（双武器 / 弹药池 / 补给品） | **未实现**（服务端 12 格背包已有，缺 Tab UI + 悬停/使用链路） | ❌ **未实现** |
| 使用物品 | **R**（悬停背包物品时使用） | **未实现**（依赖 Tab 背包 UI） | ❌ **未实现** |
| 快捷道具 | 3 / 4 快捷使用（恢复品/战术品）；**按住打开轮盘，点轮盘中心撤销** | 本轮实现为"长按开场 → **鼠标靠中心死区**取消 → 松开使用" | ⚠️ **交互形态与原版不同**（原版是*点击*轮盘中心，本轮是*死区*），需确认是否接受 |
| 关闭 / 取消 | **Esc**：关背包 / 关功能台 / 取消持雷 / **无 UI 时释放鼠标** | Esc 部分接线 | ⚠️ "无 UI 时释放鼠标（解锁指针）" **未验证** |
| 暂停 | **`/` 或 `~`** | 已实现（`~`） | ⚠️ `/` 是否也支持 **未验证** |

### B-1. 手雷"先瞄准后释放"专项（legacy 明确要求，当前缺失）

legacy 原文：**"手雷必须『先瞄准后释放』：任意途径使用后进入持握并强制越肩瞄准，左键投出 / Esc 取消放回背包。"**

- 现状：`combat::use_item_at` 的 Grenade 分支是**直接生成投掷物**（即时抛出）。
- 结论：❌ **未还原**。这是一条独立待办，不要和 3/4 轮盘混在一起做。

---

## C. 冻结规则（在本文件存续期间生效）

1. **禁止**在 A 表 1/3/4 与 B 表任一 ❌ 项之上继续叠加新玩法代码。
2. **禁止**把 `./_ref/Cute-of-Duty-0.3.2`（legacy 源码）里的模块**直接搬**进当前仓库——
   本次重构的目标是**先定边界**，搬运必须在边界文档 + 契约确定之后。
3. **禁止**在未跑 `cargo-wrap check --workspace` 的情况下声称任何改动"完成"。
4. **禁止**新增"临时兼容分支/开关"来绕过上述项；要么按边界实现，要么留在本文件里。

---

## D. 解除冻结的验证清单（逐条勾掉才可移出本文件）

> **前置（owner 2026-09-26 追加）**：冻结问题**已可控**（边界体系落地 + `check`/`test` 可跑）。
> 因此解冻顺序**固定**为：
> **① 先清理多余文件**（未被 `mod` 声明的 `.rs`、残留空目录、无引用资产与死代码）
> **→ ② 再做冻结需求**（A 表 1/3/4 + B 表逐行核对）。
> 理由：清掉冗余后才能判断"哪些断层是真断层"，避免在杂物上做验证。

- [ ] **前置**：清理多余文件（未 `mod` 的 `.rs` / 空目录 / 无引用资产与死代码）——清完才可开工冻结需求
- [ ] `cargo-wrap check --workspace` 通过（= 客户端断层已修通）
- [ ] `cargo-wrap test -p cute_of_duty_server` 全绿
- [ ] 本地双端联调：进入 lawn 训练场，验证 A1（3/4 名称+轮盘）、A3（就近常显+滚轮+按名补给）、A4（两个 4×3 搬运）
- [ ] `menu/arsenal.rs` 两个 4×3 的拖拽 / Shift 快捷移动无越界（空格位不响应）
- [ ] B 表逐行给出"验证方式 + 实测结果"，或明确降级为"本版本不还原"并写入 barek-history
